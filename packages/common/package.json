{
  "name": "@mythfood/common",
  "version": "1.0.0",
  "private": true,
  "description": "Shared utilities for MyThFood microservices",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist",
    "test": "jest --passWithNoTests",
    "test:coverage": "jest --passWithNoTests --coverage"
  },
  "dependencies": {
    "@mythfood/shared-kernel": "workspace:*",
    "reflect-metadata": "^0.2.2"
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  }
}